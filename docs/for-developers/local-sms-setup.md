# 📱 Настройка SMS (Eskiz) для локальной разработки

## 🎯 Варианты настройки

### Вариант 1: Mock режим (рекомендуется для разработки)

Не отправляет реальные SMS, только логирует в консоль.

**В `.env.local`:**
```bash
ESKIZ_MOCK=true
```

**Преимущества:**
- ✅ Не тратит деньги на SMS
- ✅ Быстро работает
- ✅ Не зависит от внешнего API
- ✅ Можно видеть OTP коды в логах

**Недостатки:**
- ❌ Не тестирует реальную отправку SMS

---

### Вариант 2: Реальная отправка SMS

Использует реальный API Eskiz.uz для отправки SMS.

**В `.env.local`:**
```bash
ESKIZ_EMAIL=ваш-email@example.com
ESKIZ_PASSWORD=ваш-пароль
ESKIZ_API_URL=https://notify.eskiz.uz/api
ESKIZ_MOCK=false  # или не указывать
```

**Где получить credentials:**
1. Зарегистрируйтесь на https://eskiz.uz/
2. Получите email и пароль
3. Убедитесь что есть баланс

**Преимущества:**
- ✅ Тестирует реальную отправку SMS
- ✅ Проверяет работу API

**Недостатки:**
- ❌ Тратит деньги на SMS
- ❌ Зависит от внешнего API
- ❌ Медленнее

---

## 🔍 Диагностика проблем

### Проблема: SMS не отправляются

**1. Проверить переменные окружения:**
```bash
# В терминале где запущен dev сервер
echo $ESKIZ_EMAIL
echo $ESKIZ_PASSWORD
```

**2. Проверить логи:**
```bash
# В консоли Next.js должны быть логи:
[Eskiz] Attempting authentication...
[Eskiz] Auth successful, token received
[Eskiz] Sending SMS...
[Eskiz] SMS response: { status: 'success' }
```

**3. Проверить ошибки:**
- `Eskiz.uz credentials not configured` → не установлены ESKIZ_EMAIL или ESKIZ_PASSWORD
- `Eskiz auth failed` → неправильный email/пароль
- `Eskiz SMS send failed` → проблема с API или балансом

**4. Проверить баланс на Eskiz.uz:**
- Зайдите на https://eskiz.uz/
- Проверьте баланс
- Если баланс 0, пополните

---

## 🛠️ Быстрая настройка

### Для локальной разработки (рекомендуется):

**1. Создать `.env.local`:**
```bash
cd edubaza-platform
cp .env.example .env.local
```

**2. Добавить в `.env.local`:**
```bash
# Mock режим - не отправляет реальные SMS
ESKIZ_MOCK=true

# Или реальная отправка (если нужно):
# ESKIZ_EMAIL=ваш-email@example.com
# ESKIZ_PASSWORD=ваш-пароль
# ESKIZ_API_URL=https://notify.eskiz.uz/api
```

**3. Перезапустить dev сервер:**
```bash
npm run dev
```

**4. Проверить логи:**
При запросе OTP в консоли должно появиться:
```
[Eskiz] MOCK MODE: SMS would be sent to +998901234567 Message: Edubaza.uz saytida ro'yxatdan o'tish uchun tasdiqlash kodi: 123456
```

---

## 📋 Чеклист настройки

- [ ] Создан `.env.local` файл
- [ ] Установлен `ESKIZ_MOCK=true` (для разработки)
- [ ] Или установлены `ESKIZ_EMAIL` и `ESKIZ_PASSWORD` (для реальной отправки)
- [ ] Dev сервер перезапущен
- [ ] Логи показывают успешную отправку или mock режим
- [ ] OTP код сохраняется в Redis (можно проверить в Redis Commander)

---

## 🔐 Безопасность

**⚠️ ВАЖНО:**
- Никогда не коммитьте `.env.local` в git
- Используйте разные credentials для development и production
- В production всегда используйте реальную отправку SMS

---

## 📞 Получение OTP кода в mock режиме

В mock режиме OTP код можно найти:

**1. В логах консоли:**
```
[Eskiz] MOCK MODE: SMS would be sent to +998901234567 Message: ... kod: 123456
```

**2. В Redis (если установлен Redis Commander):**
- Откройте http://localhost:8081
- Найдите ключ `otp:+998901234567`
- Значение = OTP код

**3. В логах API:**
```bash
# В терминале где запущен dev сервер
# При запросе OTP будет видно код
```

---

**Готово! Теперь можно разрабатывать локально без реальных SMS.**

